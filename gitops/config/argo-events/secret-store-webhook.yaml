apiVersion: external-secrets.io/v1
kind: SecretStore
metadata:
  name: webhook-secret
  namespace: argo-events
spec:
  provider:
    aws:
      service: SecretsManager
      region: us-east-1
      auth:
        jwt:
          serviceAccountRef:
            name: webhook
            namespace: argo-events