apiVersion: v1
kind: Service
metadata:
  name: echo-mcp
  labels:
    app: echo-mcp
    app.kubernetes.io/part-of: kagent-jwt-dryrun
spec:
  selector:
    app: echo-mcp
  ports:
    - name: http
      port: 8080
      targetPort: 8080
