apiVersion: v1
kind: Service
metadata:
  name: fake-llm
  labels:
    app: fake-llm
    app.kubernetes.io/part-of: kagent-jwt-dryrun
spec:
  selector:
    app: fake-llm
  ports:
    - name: http
      port: 8080
      targetPort: 8080
