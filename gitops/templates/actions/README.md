# Templates de Actions

En esta carpeta, se encuentran contenidos los templates (`ClusterWorkflowTemplates` y `WorkflowTemplates`) que embeben actions comunitarios, de GitHub y Azure, para ser consumidos en la estrategia de centralización de pipelines.

